{
  "frontend/src/lib/blobStorage.ts": "Blob storage utility class that converts raw bytes or URLs into usable blob URLs, with the `ArrayBufferLike` to `ArrayBuffer` cast fixed by routing through `unknown` to satisfy TypeScript's strict overlap check.",
  "frontend/src/App.tsx": "Main app state machine managing all screens and navigation — uses `isAdmin` and `isAuthenticated` from AuthContext (now properly exported), passes correct props to all components including `serviceName` to ServiceForm and `onViewReceipt` to OrderTracking.",
  "frontend/src/components/OrderTracking.tsx": "Order tracking component with optional `onViewReceipt` prop, 6-stage progress stepper, attachment viewer using Base64 data URIs, and loading/error states.",
  "frontend/src/components/AdminDashboard.tsx": "Admin-only dashboard with stats, tabbed order list, and per-card OrderCard components — Accept/Reject/status buttons are fully wired with inline feedback, attachment buttons open Base64 data URIs in a new window, and all error messages avoid \"re-login\" or \"authorization\" language.",
  "frontend/src/components/PaymentScreen.tsx": "Payment screen showing either the admin-uploaded permanent QR image or a canvas-generated UPI QR code with payment instructions for customers.",
  "frontend/src/components/AdminQRManagement.tsx": "Admin QR management panel with optional `onBack` prop, Permanent QR upload and Auto QR amount tabs — all error messages avoid re-login language, access controlled by `isAdminLoggedIn`.",
  "frontend/src/components/Receipt.tsx": "Receipt component that fetches order by ID, renders order details, progress bar, amount, and attachment buttons using Base64 data URIs opened in a new window (no file-path URLs).",
  "frontend/src/components/Contact.tsx": "Contact page with prominent phone number display and action buttons for WhatsApp chat, direct call, and Google Maps navigation.",
  "frontend/src/components/VijayAIChatbot.tsx": "Floating chatbot button with slide-up chat panel implementing rule-based Hindi/English responses for services, payments, tracking, and contact queries.",
  "frontend/src/components/Layout.tsx": "App shell with top bar, bottom navigation, and a logout button that calls both the AuthContext logout function AND the onLogout navigation callback to properly redirect the user to the login screen.",
  "frontend/src/contexts/AuthContext.tsx": "AuthContext with persistent localStorage-based session for both admin and customer — exports both `isAdminLoggedIn` and `isAdmin` (alias), `setCustomerSession` (backward-compat alias for `customerLogin`), and `isAuthenticated` so all existing components continue to compile without changes.",
  "frontend/src/components/ServiceForm.tsx": "Service application form with optional `serviceName` prop for pre-selection, service grid, personal details, file attachments converted to Base64, inline success screen with order ID, and improved error messages.",
  "frontend/src/components/CustomerLogin.tsx": "Customer login/register screen with manual and OTP login modes; on successful login saves the customer session to AuthContext (which persists it to localStorage), and includes password length validation on registration.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations — restores `useLoginCustomer` and `useRegisterCustomer` exports, `useGetAllOrders` returns empty array on auth errors, `useSubmitOrder` has retry logic, and `useUpdateOrderStatus`/`useConfirmPayment` accept `bigint` orderId with proper cache invalidation.",
  "frontend/src/components/MyOrders.tsx": "Customer orders list screen that sorts orders by orderId descending (replacing the removed timestamp field), displays status badges, and provides Track Order and View Receipt buttons for each order."
}